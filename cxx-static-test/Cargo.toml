[package]
name = "cxx-static-test"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["staticlib"]

[dependencies]
cxx = { path = ".." }

[build-dependencies]
cxx-build = { path = "../gen/build" }
